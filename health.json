{
  "run": [
    {
      "method": "net.request",
      "params": {
        "url": "http://localhost:11434/health",
        "method": "GET"
      }
    },
    {
      "method": "log",
      "params": {
        "json2": "{{input}}"
      }
    }
  ]
}
